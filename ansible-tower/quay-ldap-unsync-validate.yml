# Validate inputs for LDAP team unsync.

- name: Run - Quay - Team LDAP Unsync - Validate
  hosts: localhost
  gather_facts: false
  force_handlers: true

  tasks:
    - name: Check required arguments
      assert:
        that: "{{ param }} is defined and {{ param }} | length"
      loop:
        - survey_quay_instance
        - survey_organization_name
        - survey_team_name
      loop_control:
        loop_var: param

    - name: Display validation summary
      debug:
        msg: |
          WARNING: Quay Team LDAP Unsync
          Instance:     {{ survey_quay_instance }}
          Organization: {{ survey_organization_name }}
          Team name:    {{ survey_team_name }}
          This will disconnect the team from its LDAP group!
